# Atlas SR Model Partition

This directory is flashed into the `model` SPIFFS partition.

Current status: placeholder only. Put ESP-SR `srmodels.bin` or generated WakeNet model assets here before enabling WakeNet/AEC as a production feature.
